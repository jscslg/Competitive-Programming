{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BUILD AND RUN",
            "type": "shell",
            "command": "python3 run.py ${file} ",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
